# TODO: Turma Status Filter Implementation

## Steps

- [x] 1. Dashboard — Exclude `concluida`/`cancelada` turmas from occupancy chart (`src/lib/utils.ts`)
- [x] 2. Dashboard — Exclude `concluida`/`cancelada` turmas from seller ranking (`src/hooks/useSalesMetrics.ts`)
- [x] 3. Turmas page — Set default filter to "Ativo" with options: Ativo, Cancelada, Concluída, Todos (`src/pages/Turmas.tsx`)
- [x] 4. Products page — Add turma status filter (default "Ativo") and show turma status on cards (`src/pages/Products.tsx`, `src/components/products/ProductCard.tsx`)

